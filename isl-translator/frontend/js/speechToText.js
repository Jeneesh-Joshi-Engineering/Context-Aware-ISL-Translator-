// Web Speech API adapter used by the transit official's screen.
export function createSpeechToText({onFinal,onError=()=>{}}) { const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition; if(!Recognition)return {supported:false,startListening(){onError("Speech recognition is not supported by this browser.")},stopListening(){}}; const recognition=new Recognition(); recognition.continuous=false;recognition.interimResults=false;recognition.onresult=e=>onFinal(e.results[e.results.length-1][0].transcript);recognition.onerror=e=>onError(e.error); return {supported:true,startListening:()=>recognition.start(),stopListening:()=>recognition.stop()}; }
